<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Diskapp - Artists</title>
</head>

<body>
    <header>
        <h1>Diskapp</h1>
    </header>
    <aside>
        <nav>
            <a href="/">Accueil</a>
            <a href="/disks">Disques</a>
            <a href="/labels">Labels</a>
            <a href="" class="btn btn-success">Ajouter un disque</a>
            <a href="" class="btn btn-info">Gérer les données annexes</a>
            <a href="/import" class="btn btn-info">Import massif</a>
        </nav>
    </aside>
        <div>
            <section>
                <!--Options-->
            </section>
            <section>
                <!--Colonnes-->
            </section>
                <!--Items-->
                {% for artist in artists %}
                <div>
                    <input type="checkbox" name="{{ artist.uuid }}" id="{{ artist.uuid }}">
                    <span>{{ artist.name }}</span>
                    <span>{{ artist.name }}</span>
                    <span>{{ artist.country }}</span>
                    <span>{{ artist.registered }}</span>
                </div>
                {% else %}
                <div><i>Nothing to show...</i></div>
                {% endfor %}
            <section>
                
            </section>
        </div>
    </main>
</body>
</html>